/**
 * 
 */

exports.build = function (logger, date, chain) {
	return function (req, res) {
		logger.log("[%s] %s %s", date(), req.method, req.url);
		chain({
			'req' : req,
			'res' : res
		});
	}
}


